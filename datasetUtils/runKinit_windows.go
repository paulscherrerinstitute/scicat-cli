package datasetUtils

import (
	"log"
)

func RunKinit(username string, password string) (err error) {
	log.Printf("On Windows make sure that you have a Kerberos ticket before continuing\n")
	return nil
}
